HUBSPOT_CHURCHILL_PAT=
ALLOWED_ORIGIN=https://refer.churchilleducation.edu.au
# When DRY_RUN=true, reads (alumni classification) still hit HubSpot,
# but writes are logged to the server console instead of POSTed.
# Use for local demos and PR previews. NEVER set this in production.
DRY_RUN=false
